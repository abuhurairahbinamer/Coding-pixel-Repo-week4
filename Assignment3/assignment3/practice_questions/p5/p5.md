# Server code can access all environment variables, including secrets such as API_KEY. Client Components only receive variables prefixed with NEXT_PUBLIC_. This prevents server secrets from accidentally being sent to the browser.

# check is applied in form.tsx